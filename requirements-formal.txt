# Python dependencies for formal verification (Halmos).
#
# Installed only by `.github/workflows/formal.yml`, which provisions Python
# 3.12. Halmos requires Python >=3.11, so this file is kept separate from
# `requirements-dev.txt` (which must remain Python 3.9-compatible — see the
# header of that file).
#
# When bumping `halmos`, also bump the `lib/halmos-cheatcodes` submodule in
# lockstep so the Python tool and the Solidity cheatcode sources stay
# compatible. See `docs/FOUNDRY_MAKEFILE_GUIDE.md`.

halmos==0.3.3
