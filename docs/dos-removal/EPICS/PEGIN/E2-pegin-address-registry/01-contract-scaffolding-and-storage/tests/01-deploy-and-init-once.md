# Test: Deploys and initializes once

**Parent:** E2.1 Contract scaffolding and storage
**Type:** Foundry test

## Asserts
- The contract deploys behind its proxy and `initialize` succeeds once.
- A second `initialize` call reverts.

Path: `test/pegin-registry/Scaffolding.t.sol`.
