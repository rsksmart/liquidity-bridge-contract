# Task: Deployment script

**Parent:** E2.1 Contract scaffolding and storage
**Type:** Task

## Inputs
- The proxy/deploy pattern used by `script/deployment/DeployPegIn.s.sol`.

## Steps
1. Add `lbc/script/deployment/DeployPegInAddressRegistry.s.sol` mirroring the existing deploy scripts (proxy + initialize).

## Done when
The script runs against the local node and prints the deployed proxy address.
