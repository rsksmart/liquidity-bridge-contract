# Test: Batch getter

**Parent:** E2.5 Federation-change derivation and batch getters
**Type:** Foundry test

## Asserts
- `getPegInAddresses([a, b])` returns the same derivations as calling `getPegInAddress` on each.

Path: `test/pegin-registry/Federation.t.sol`.
