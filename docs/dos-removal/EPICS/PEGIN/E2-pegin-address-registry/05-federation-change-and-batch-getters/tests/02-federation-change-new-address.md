# Test: Federation change yields a new address

**Parent:** E2.5 Federation-change derivation and batch getters
**Type:** Foundry test

## Asserts
- For one RSK address, the derived address differs before and after a simulated powpeg composition change.

Path: `test/pegin-registry/Federation.t.sol`.
