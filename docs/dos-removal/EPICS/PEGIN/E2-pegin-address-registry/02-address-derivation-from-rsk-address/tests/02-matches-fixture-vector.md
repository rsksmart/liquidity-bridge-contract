# Test: Matches an off-chain fixture vector

**Parent:** E2.2 Address derivation from RSK address
**Type:** Foundry test

## Asserts
- For a fixed RSK address and a fixed powpeg redeem script, the derived address equals a vector computed off-chain with the same scheme.

Path: `test/pegin-registry/Derivation.t.sol`.
