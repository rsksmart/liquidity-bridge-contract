# Test: Permissionless caller

**Parent:** E2.4 Deposit-gated permissionless registration
**Type:** Foundry test

## Asserts
- A caller other than the owner of `addr` can register it with a valid deposit proof (no signature required).

Path: `test/pegin-registry/Register.t.sol`.
