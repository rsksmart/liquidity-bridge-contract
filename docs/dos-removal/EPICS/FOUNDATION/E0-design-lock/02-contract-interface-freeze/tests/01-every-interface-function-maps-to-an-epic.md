# Test: Every interface function maps to an epic that uses it (E1, E2, E7)

**Parent:** S0.2 Contract interface freeze
**Type:** Review gate (design)

Every interface function maps to an epic that uses it (E1, E2, E7).
