# Task: Set getters

**Parent:** E3.1 Registered-LP enumeration and registration block
**Type:** Task

## Steps
1. Implement `registeredLPCount()` and `isRegisteredLP(address)` over the set.

## Done when
`forge test --match-path test/collateral/Registry.t.sol` covers the getters and passes.
