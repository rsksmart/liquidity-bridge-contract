# Task: Add `fixedFee` and `percentageFee` storage and getters per flow

**Parent:** E1.2 Fee model (fixed floor plus percentage)
**Type:** Task

Add `fixedFee` and `percentageFee` storage and getters per flow.
